RSpec.describe AnsibleTowerClient do
  it "has a version number" do
    expect(AnsibleTowerClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
